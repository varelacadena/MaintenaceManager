/**
 * @deprecated Prefer `useWorkAdmin` or `useWorkField` directly from `index.tsx`.
 * Kept for any external imports.
 */
export { useWorkAdmin as useWork, type WorkContext } from "./useWorkAdmin";
