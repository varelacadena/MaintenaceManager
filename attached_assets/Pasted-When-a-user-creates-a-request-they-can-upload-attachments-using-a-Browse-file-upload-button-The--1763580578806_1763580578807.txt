When a user creates a request, they can upload attachments using a “Browse” file-upload button. The files upload during creation, but after the request is submitted, the following problems occur:

ISSUES TO FIX
1. Attachments do NOT appear on the Request Details page

Uploaded files are not being displayed.

The attachments section shows no files, even though files were uploaded during submission.

2. The “Browse” / file-picker button still appears after submission

The file upload control should only appear while the request is being created or edited.

On the Request Details page (read-only view), the “Browse” button must be hidden.

WHAT I NEED THE AI TO DO

Please review my code for:

The frontend upload component

The backend upload handler / route

The database model or storage logic

The request details rendering component

Any missing reference to the uploaded file list

Then:

REQUIRED FIXES
A. Fix attachment persistence

Ensure uploaded files are:

Successfully stored (filesystem or cloud)

Properly saved in the database (filename, path, metadata)

Correctly linked to the specific request ID

Return the file data in the API response when viewing a request.

B. Display attachments on the Request Details page

List all files uploaded for the request.

Provide a link or button to view/download each file.

Format them in a clean attachments section.

C. Hide the file picker after submission

The Request Details page must be read-only.

Remove/hide the “Browse” button and file-upload UI unless the user is in edit mode.

D. Provide code updates for:

The upload component

The backend endpoint

The database model (if needed)

The Request Details UI

Any helper utilities that manage attachments

E. Explain what caused the bug

Also include a short explanation of:

Why attachments failed to show

Why the browse button stayed visible

How your solution prevents the problem in the future

✔️ EXPECTED FINAL BEHAVIOR

Uploads save correctly and are tied to the request.

Attachments are visible in the request details view.

The browse/upload button only shows during creation or editing—not after submission.

The system works consistently for all user roles.